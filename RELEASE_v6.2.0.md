# PVM Standard 6.2.0

6.2.0は、baseline / lock / unlockの運用構造を変えず、初回baselineの「ICA①次元とkの探索」と出力の意味づけを修正するリリースです。

## 変更点

- PCA次元全体を対数間隔で覆うICA①候補を探索し、必要な範囲だけ局所refineします。
- retry中に収束した実効次元をcanonical候補として登録し、比較時は次元fallbackを無効化します。canonical次元に収束しないseedはARI計算から除外し、失敗理由と成功率を出力します。
- Centroid ProjectionのrankがICA①次元より小さいことを完全版候補の資格条件にします。削減次元数そのものは選択スコアへ加点しないため、小さいkを直接優遇しません。
- ICA①軸はseed再現性、列シャッフル対照との差、極端文集合とSpearman相関による非重複性を診断します。これは意味内容の自動証明ではなく、人が読む軸を選ぶ補助情報です。
- CPは、割当変更率・境界例への集中・コア文書の保存を診断します。CP後の内部指標だけを自己評価に使いません。
- 完全版が成立しない場合の既存ICA/PCA fallbackは残しますが、`selection_tier=degraded`、`quality_gate_passed=false` として完全版から分離します。
- `結果スコア.csv` の完全版最終座標を `CP1...` と明記し、ICA①座標と区別します。初回baselineでは `ICA軸レポート.md` を既定出力し、`--include-ica1-cols` でCSVにもICA①座標を追加できます。
- `--search-budget fast|standard|thorough` を追加しました。

## 互換性

- `SCRIPT_VERSION`: `PVM-standard-6.2.0`
- baseline schema: `2.1`（6.1.2と同じ）
- 既存のbaseline / lock / unlockコマンドは維持します。
- `--max_ic_cols` は互換維持し、意味が明確な別名 `--max-cp-cols` を追加しました。

## 評価上の注意

候補選択スコアは同一データ内の探索順位です。異なるデータセット間の絶対品質を示す普遍スコアではありません。クラスタ代表文、ICA①軸、CPによって所属が変わった境界文、holdout lockの挙動を合わせて確認してください。

6.2.0のseed安定性は、同じ全データに対する初期値変更を測ります。train標本そのものを変えたときのlock安定性は別の性質であり、候補スコアにはまだ含めません。重要なbaselineでは複数holdout分割も確認してください。

## 既知の退化

ももクロ実テキスト5000件・同一10分割での確認では、6.1.2が選んだd=2/k=4はholdout lock ARI平均0.927・最小0.880、6.2.0が選んだd=6/k=4は平均0.822・最小0.427でした。後者は中央値0.909で10分割中8分割が0.7以上でしたが、2分割で0.5未満です。これは6.1.2から元々あった崩れではなく、CPが実効しない低次元表現から複数のICA①軸を保持する表現へ選択を変えた際に観測された再標本感度です。6.2.0は意味軸とCPの実効性を回復する一方、低次元候補が持っていた見かけ上のlock安定性を常に維持するとは限りません。再標本lock安定性の候補採点は6.3.0で扱います。

strict_full採用時は実行ログに警告し、`結果レポート.json`の`lock_resample_caveat`にも制約・回避策・6.3.0での対応予定を記録します。
