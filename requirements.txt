# --- Core ---
numpy>=2.1,<3
pandas>=2.2.3,<3
scikit-learn>=1.6,<2

# --- Embedding (Ruri v3 系で必須) ---
# CPU wheel index is included to avoid pulling heavy CUDA builds where a CPU wheel is available.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.6,<3
transformers>=4.45,<5
huggingface_hub>=0.24,<1
sentencepiece>=0.2,<1
protobuf>=4,<6

# --- Utils / IO ---
tqdm>=4.66,<5
openpyxl>=3.1,<4
joblib>=1.4,<2

# --- Optional ---
# Hugging Face の Xet Storage 警告を抑えたい/初回DLを改善したい場合は手元で追加導入してください。
# pip install hf_xet
